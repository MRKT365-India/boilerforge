# BOILERFORGE Workflow (Claude)

This project is managed by Boilerforge workflow packs.

## Managed Paths

- `BOILERFORGE.md`
- `.claude/commands/`
- `.claude/agents/`
- `.claude/skills/`

Use `boilerforge update` to refresh workflow assets.
Use `boilerforge protect add <category> <name>` to preserve local customizations during updates.
